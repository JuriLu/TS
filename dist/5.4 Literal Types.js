"use strict";
//* Literal Type
let zero = 0; //* The type is exactly the number 0
// zero = 1
const giveAnswer = (answer) => {
    return `The answer is ${answer}`;
};
giveAnswer('no');
let today = 'Monday';
// today = 'weds' //! Won't work
